
| `wget -O <new_name> <url>` | download & rename file                                   |
| -------------------------- | -------------------------------------------------------- |
| `sudo dpkg -i <file.deb>`  | install a .deb file (~ archiving format similar to .jar) |
